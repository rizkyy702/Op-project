procs:
  supersim:
    cwd: .
    shell: pnpm supersim:start
  supersim-chain-901:
    cwd: supersim-logs
    shell: pnpm wait-port http://:8420/ready && tail -f anvil-901.log
  supersim-chain-902:
    cwd: supersim-logs
    shell: pnpm wait-port http://:8420/ready && tail -f anvil-902.log
  frontend:
    cwd: .
    shell: pnpm dev:frontend
  deploy-contracts:
    cwd: .
    shell: pnpm wait-port http://:8420/ready && pnpm deploy:sup:supersim
